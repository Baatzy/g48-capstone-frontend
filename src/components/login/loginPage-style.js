const centerWellLogin = {
  position: "absolute",
  top: "50%",
  transform: "translateY(50%)",
  // opacity: "0.5",
}

const centerWellSignup = {
  position: "absolute",
  top: "50%",
  transform: "translateY(39%)",
  // opacity: "0.5",
}

const parentElement = {
  position: "relative",
}

let backgroundImage = '/images/crimp-hand.jpg'

const logoFont = {
  color: 'white'
}

export { parentElement, centerWellLogin, centerWellSignup, backgroundImage, logoFont }
